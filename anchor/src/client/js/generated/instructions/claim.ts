/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { VESTING_SOL_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CLAIM_DISCRIMINATOR = new Uint8Array([
  62, 198, 214, 193, 213, 159, 108, 210,
]);

export function getClaimDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLAIM_DISCRIMINATOR);
}

export type ClaimInstruction<
  TProgram extends string = typeof VESTING_SOL_PROGRAM_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVestingRecord extends string | AccountMeta<string> = string,
  TAccountVestingStorage extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountVestingRecord extends string
        ? WritableAccount<TAccountVestingRecord>
        : TAccountVestingRecord,
      TAccountVestingStorage extends string
        ? WritableAccount<TAccountVestingStorage>
        : TAccountVestingStorage,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimInstructionData = { discriminator: ReadonlyUint8Array };

export type ClaimInstructionDataArgs = {};

export function getClaimInstructionDataEncoder(): FixedSizeEncoder<ClaimInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLAIM_DISCRIMINATOR })
  );
}

export function getClaimInstructionDataDecoder(): FixedSizeDecoder<ClaimInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClaimInstructionDataCodec(): FixedSizeCodec<
  ClaimInstructionDataArgs,
  ClaimInstructionData
> {
  return combineCodec(
    getClaimInstructionDataEncoder(),
    getClaimInstructionDataDecoder()
  );
}

export type ClaimAsyncInput<
  TAccountOwner extends string = string,
  TAccountVestingRecord extends string = string,
  TAccountVestingStorage extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vestingRecord: Address<TAccountVestingRecord>;
  vestingStorage?: Address<TAccountVestingStorage>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getClaimInstructionAsync<
  TAccountOwner extends string,
  TAccountVestingRecord extends string,
  TAccountVestingStorage extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VESTING_SOL_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ClaimAsyncInput<
    TAccountOwner,
    TAccountVestingRecord,
    TAccountVestingStorage,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ClaimInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVestingRecord,
    TAccountVestingStorage,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? VESTING_SOL_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vestingRecord: { value: input.vestingRecord ?? null, isWritable: true },
    vestingStorage: { value: input.vestingStorage ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.vestingStorage.value) {
    accounts.vestingStorage.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            118, 101, 115, 116, 105, 110, 103, 95, 115, 116, 111, 114, 97, 103,
            101,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.vestingRecord.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.vestingRecord),
      getAccountMeta(accounts.vestingStorage),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getClaimInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVestingRecord,
    TAccountVestingStorage,
    TAccountSystemProgram
  >);
}

export type ClaimInput<
  TAccountOwner extends string = string,
  TAccountVestingRecord extends string = string,
  TAccountVestingStorage extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vestingRecord: Address<TAccountVestingRecord>;
  vestingStorage: Address<TAccountVestingStorage>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getClaimInstruction<
  TAccountOwner extends string,
  TAccountVestingRecord extends string,
  TAccountVestingStorage extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VESTING_SOL_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ClaimInput<
    TAccountOwner,
    TAccountVestingRecord,
    TAccountVestingStorage,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ClaimInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountVestingRecord,
  TAccountVestingStorage,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? VESTING_SOL_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vestingRecord: { value: input.vestingRecord ?? null, isWritable: true },
    vestingStorage: { value: input.vestingStorage ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.vestingRecord),
      getAccountMeta(accounts.vestingStorage),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getClaimInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVestingRecord,
    TAccountVestingStorage,
    TAccountSystemProgram
  >);
}

export type ParsedClaimInstruction<
  TProgram extends string = typeof VESTING_SOL_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    vestingRecord: TAccountMetas[1];
    vestingStorage: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ClaimInstructionData;
};

export function parseClaimInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedClaimInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      vestingRecord: getNextAccount(),
      vestingStorage: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getClaimInstructionDataDecoder().decode(instruction.data),
  };
}
