/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from 'gill';
import { VESTING_SOL_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPDATE_TREASURY_DISCRIMINATOR = new Uint8Array([
  60, 16, 243, 66, 96, 59, 254, 131,
]);

export function getUpdateTreasuryDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_TREASURY_DISCRIMINATOR
  );
}

export type UpdateTreasuryInstruction<
  TProgram extends string = typeof VESTING_SOL_PROGRAM_PROGRAM_ADDRESS,
  TAccountVesting extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVesting extends string
        ? WritableAccount<TAccountVesting>
        : TAccountVesting,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateTreasuryInstructionData = {
  discriminator: ReadonlyUint8Array;
  newTreasury: Address;
};

export type UpdateTreasuryInstructionDataArgs = { newTreasury: Address };

export function getUpdateTreasuryInstructionDataEncoder(): FixedSizeEncoder<UpdateTreasuryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newTreasury', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_TREASURY_DISCRIMINATOR })
  );
}

export function getUpdateTreasuryInstructionDataDecoder(): FixedSizeDecoder<UpdateTreasuryInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newTreasury', getAddressDecoder()],
  ]);
}

export function getUpdateTreasuryInstructionDataCodec(): FixedSizeCodec<
  UpdateTreasuryInstructionDataArgs,
  UpdateTreasuryInstructionData
> {
  return combineCodec(
    getUpdateTreasuryInstructionDataEncoder(),
    getUpdateTreasuryInstructionDataDecoder()
  );
}

export type UpdateTreasuryInput<
  TAccountVesting extends string = string,
  TAccountOwner extends string = string,
> = {
  vesting: Address<TAccountVesting>;
  owner: TransactionSigner<TAccountOwner>;
  newTreasury: UpdateTreasuryInstructionDataArgs['newTreasury'];
};

export function getUpdateTreasuryInstruction<
  TAccountVesting extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof VESTING_SOL_PROGRAM_PROGRAM_ADDRESS,
>(
  input: UpdateTreasuryInput<TAccountVesting, TAccountOwner>,
  config?: { programAddress?: TProgramAddress }
): UpdateTreasuryInstruction<TProgramAddress, TAccountVesting, TAccountOwner> {
  // Program address.
  const programAddress =
    config?.programAddress ?? VESTING_SOL_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vesting: { value: input.vesting ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vesting),
      getAccountMeta(accounts.owner),
    ],
    data: getUpdateTreasuryInstructionDataEncoder().encode(
      args as UpdateTreasuryInstructionDataArgs
    ),
    programAddress,
  } as UpdateTreasuryInstruction<
    TProgramAddress,
    TAccountVesting,
    TAccountOwner
  >);
}

export type ParsedUpdateTreasuryInstruction<
  TProgram extends string = typeof VESTING_SOL_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vesting: TAccountMetas[0];
    owner: TAccountMetas[1];
  };
  data: UpdateTreasuryInstructionData;
};

export function parseUpdateTreasuryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateTreasuryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { vesting: getNextAccount(), owner: getNextAccount() },
    data: getUpdateTreasuryInstructionDataDecoder().decode(instruction.data),
  };
}
